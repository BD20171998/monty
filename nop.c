#include "monty.h"
/**
 * nop - nothing to add or do
 * @stack: stack of elements
 * @line_number: line number
 */
void nop(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
}
